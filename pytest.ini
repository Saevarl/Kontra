[pytest]
addopts = -q
markers =
    slow: uses ~1e6 rows or heavy I/O
    integration: end-to-end engine tests
    pushdown: tests that toggle pushdown
    projection: tests that assert req/loaded/avail
    lazy_loading: guards lazy loading invariant (must always pass)
